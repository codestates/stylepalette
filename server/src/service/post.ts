import { IPost, postUniqueSearchInput } from "../interfaces/IPost";
import {Post} from "../models/post";

const getpost = async () => {
 
}

const getposts = async () => {
 
}

const postpost = async () => {
 
}

const deletepost = async () => {
 
}

export default {
  getpost,
  getposts,
  postpost,
  deletepost
};